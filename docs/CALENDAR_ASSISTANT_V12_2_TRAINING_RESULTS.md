# V12.2: итог обучения и проверки

Обучение завершено 2026-09-07 с кодом выхода 0, проверка модели завершена 2026-09-08 с кодом выхода 0. Выполнены 119 из 119 шагов, одна эпоха. Адаптер сохранён отдельно. Результат не является безусловной заменой V12.1: общий строгий счёт вырос, но появились регрессии.

## Данные и обучение

- Вручную добавлены ровно 48 примеров за 2027-2032 годы: 24 високосных и 24 обычных случая.
- Новый слой: 32 train, 8 validation, 8 holdout. Полный набор: 1904 train и 549 validation. Прежние 63 holdout-примера сохранены отдельно и побайтно.
- Все даты новых примеров проверены календарными утверждениями. Генератор обучающих примеров не запускался.
- База: Qwen3-4B-Instruct-2507, revision `cdbee75f17c01a7cc42f958dc650907174af0554`. Обучение от исходной базы, не продолжение старого адаптера.
- Параметры прежние: QLoRA NF4, FP16 на GTX 1080 Ti, rank 16, alpha 32, dropout 0.05, learning rate 0.0001, batch 1, accumulation 16, sequence length 256, seed 20260825.
- Train loss: `0.2412694592`. Final validation loss: `0.0921219811`.
- Train runtime: `2434.6455` секунды; финальная проверка validation: ещё `190.737` секунды.
- Loss не является точностью ответов. Его нельзя напрямую сопоставлять с loss V12.1: состав validation изменился.

## Новые календарные тесты

| Проверка | Результат |
| --- | ---: |
| Синтаксически корректный JSON | 8/8 |
| Контракт ответа | 7/8 |
| Правильный intent | 8/8 |
| Полный строгий проход | 5/8 |
| Точное совпадение календарных полей | 5/8 |
| Ожидаемый краткий reply | 8/8 |
| Високосные случаи | 3/4 |
| Обычные годы | 2/4 |

Оставшиеся три ошибки:

1. **V122H04, 2032 год:** «через десять минут» от `2032-02-29T23:55` должно дать `2032-03-01T00:05`. Модель оставила исходное `2032-02-29T23:55`.
2. **V122H06, 2029 год:** «через двадцать пять минут» от `2029-02-28T23:45` должно дать `2029-03-01T00:10`. Модель вернула только дату `2029-03-01`, потеряв время.
3. **V122H07, 2030 год:** «послезавтра» от 27 февраля должно дать `2030-03-01`. Модель вернула несуществующую дату `2030-02-29`.

V12.1 на этих восьми новых случаях отдельно не запускалась, поэтому сравнение версий на новом holdout не заявляется.

## Прежние 63 теста

Проверка выполнена на том же побайтно сохранённом holdout, с прежними настройками инференса: 4-битный базовый checkpoint с адаптером, greedy decoding, batch 4, максимум 128 новых токенов. Совпадение нового кода оценки со старым отчётом V12.1 проверено на сохранённых предсказаниях.

| Проверка | V12.1 | V12.2 |
| --- | ---: | ---: |
| Корректный JSON | 63/63 | 63/63 |
| Контракт ответа | 59/63 | 60/63 |
| Правильный intent | 60/63 | 61/63 |
| Точные intent/params без требования к reply | 34/63 | 38/63 |
| Полный строгий проход | 33/63 | 36/63 |
| Проход с послаблением только по регистру текстовых полей | 35/63 | 38/63 |
| Ожидаемый краткий reply создания | 23/25 | 22/25 |

Строгий проход означает допустимый контракт и точные intent/params. Для создания reply должен совпадать с названием. Для других intent допускается иная формулировка reply внутри прежнего контракта. Смысловые альтернативы поисковых строк не засчитываются автоматически.

Семь ранее не проходивших случаев теперь прошли: **H004, H024, H047, H051, H060, H061, H12105**. В частности, H004 теперь правильно возвращает `2028-02-29T15:00`. H008 и H011 также проходят.

Четыре ранее проходивших случая теперь не прошли:

- **H039:** при переносе урока потерян исходный диапазон дат, запрос сокращён с `урок алгебры` до `урок`.
- **H050:** расписание попало в название и reply: `Поезд до Сызрани послезавтра в шесть сорок утра`. Это регрессия основного правила V12.1, несмотря на верные отдельные календарные поля.
- **H057:** при суммировании без указанного периода модель самостоятельно добавила сегодняшний диапазон.
- **H12104:** вместо даты без времени модель добавила `00:00` и переформулировала название.

Кроме того, всё ещё не проходящий **H12101** теперь возвращает `Послезавтра.` в reply при правильном названии `К парикмахеру`. Этот случай не входит в четыре новых провала, поскольку и V12.1 не проходила его строго, но нарушение краткого reply требует отдельного внимания.

Итог на старом наборе: 7 исправленных случаев и 4 новых провала, чистое изменение +3. Осталось 27 строгих несовпадений. Вместе с новым holdout это 30 несовпадений на 71 случае, но эти наборы следует оценивать раздельно. Устранение календарных ошибок и отсутствие регрессий не достигнуты полностью. После просмотра результатов дополнительное обучение и подгонка эталонов не проводились.

## Артефакты и границы

- Адаптер: `build/calendar_sft_qwen3_v12_2_epoch1_20260907/adapter/adapter_model.safetensors`.
- Размер: `132187888` байт, 504 тензора. Проверены все значения: NaN/Inf отсутствуют.
- SHA-256 адаптера: `81ac921094ac02311386fc1082acfafb5e434a95fb20690534af6842760b4a96`.
- Запуск и метрики: `build/calendar_sft_qwen3_v12_2_epoch1_20260907/run_manifest.json`.
- Новые ответы и оценка: `holdout_predictions.jsonl`, `holdout_report.json` в той же папке запуска.
- Прежние тесты: `regression_holdout_predictions.jsonl`, `regression_holdout_report.json` в той же папке.
- В отчётах сохранены все ожидаемые/полученные ответы, временной контекст, параметры инференса и хеши.
- Пройдены 176 тестов, включая проверки данных V12.2 и кода оценки.

Адаптер V12.1 проверен по прежнему SHA-256 и не изменён. Android-код, исходные наборы и прежние GGUF не изменялись. GGUF V12.2 не создавались. Инференс выполнялся только для получения ответов на уже существующие тесты, без создания обучающих данных. Генеративная оценка всех 549 validation-примеров и тестирование на Android не проводились.
